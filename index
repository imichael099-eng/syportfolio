import React from 'react';
import { motion } from 'framer-motion';
import { Github, Linkedin, Mail, ExternalLink, Code, Shield, Cpu, Layers, Phone } from 'lucide-react';

// Replace 'profile.jpg' with the actual filename of the photo you uploaded
const profileImg = "profile.jpg"; 
const electricityDashboardImg = "electricity-dashboard.png";

const S_Y_Portfolio = () => {
  const skills = [
    "Python", "JavaScript", "React", "TypeScript", 
    "Vue.js", "Laravel PHP", "WordPress", "MySQL", 
    "Project Management", "Cybersecurity", "UI/UX Design"
  ];

  const projects = [
    { 
      name: "Electricity Dashboard", 
      tag: "Data Analytics", 
      desc: "An enterprise-level visualization tool for monitoring energy consumption patterns.",
      img: electricityDashboardImg
    },
    { 
      name: "Food Delivery E-commerce", 
      tag: "Full Stack", 
      desc: "A high-performance platform featuring real-time tracking and secure payment gateways." 
    },
    { 
      name: "Real Estate Canada", 
      tag: "Commercial Web", 
      desc: "A sophisticated property listing engine optimized for Canadian market SEO standards." 
    },
    { 
      name: "Legal Firm Portal", 
      tag: "Professional Services", 
      desc: "A secure, encrypted document management system and client intake platform." 
    }
  ];

  const fadeInUp = {
    initial: { opacity: 0, y: 30 },
    whileInView: { opacity: 1, y: 0 },
    viewport: { once: true },
    transition: { duration: 0.8, ease: "easeOut" }
  };

  return (
    <div className="min-h-screen bg-[#020617] text-slate-400 font-sans selection:bg-cyan-500/30 overflow-x-hidden">
      
      {/* Background Graphics / Particles */}
      <div className="fixed inset-0 pointer-events-none">
        <div className="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-cyan-500/10 rounded-full blur-[120px]" />
        <div className="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-blue-500/10 rounded-full blur-[120px]" />
      </div>

      {/* Animated Navigation */}
      <motion.nav 
        initial={{ y: -100 }}
        animate={{ y: 0 }}
        className="fixed top-0 w-full z-50 bg-[#020617]/80 backdrop-blur-xl border-b border-slate-800/50"
      >
        <div className="max-w-7xl mx-auto px-8 py-5 flex justify-between items-center">
          <motion.span 
            whileHover={{ scale: 1.1 }}
            className="text-2xl font-black tracking-tighter text-white uppercase border-2 border-cyan-500 px-2 py-1"
          >
            SY
          </motion.span>
          <div className="hidden md:flex gap-10 text-xs font-bold uppercase tracking-widest">
            {['About', 'Skills', 'Projects', 'Contact'].map((item) => (
              <a key={item} href={`#${item.toLowerCase()}`} className="hover:text-cyan-400 transition-colors">
                {item}
              </a>
            ))}
          </div>
        </div>
      </motion.nav>

      {/* Hero Section */}
      <section id="about" className="relative pt-48 pb-32 px-8">
        <div className="max-w-7xl mx-auto grid md:grid-cols-2 gap-12 items-center">
          <motion.div {...fadeInUp}>
            <span className="text-cyan-500 font-mono tracking-widest uppercase mb-4 block">Software Engineer</span>
            <h1 className="text-6xl md:text-8xl font-bold text-white mb-8 tracking-tight">
              Building digital <br />
              <span className="text-slate-500 italic font-light">experiences.</span>
            </h1>
            <p className="text-xl leading-relaxed max-w-xl mb-12">
              I am <span className="text-white font-bold">S.Y MICHAEL</span>, a Software Engineer bridging the gap between data and aesthetics to solve real-world challenges.
            </p>
            <div className="flex gap-4">
               <a href="mailto:imichael099@gmail.com" className="bg-cyan-500 text-white px-8 py-4 rounded-lg font-bold hover:bg-cyan-400 transition shadow-lg shadow-cyan-500/20">Hire Me</a>
               <a href="#projects" className="border border-slate-700 px-8 py-4 rounded-lg font-bold hover:bg-slate-800 transition">View Projects</a>
            </div>
          </motion.div>

          <motion.div 
            initial={{ opacity: 0, scale: 0.8 }}
            whileInView={{ opacity: 1, scale: 1 }}
            transition={{ duration: 1 }}
            className="relative"
          >
            <div className="relative z-10 w-full aspect-square rounded-2xl overflow-hidden border border-slate-700 grayscale hover:grayscale-0 transition-all duration-700">
              <img src={profileImg} alt="S.Y Michael" className="w-full h-full object-cover" />
            </div>
            <div className="absolute -bottom-6 -right-6 w-full h-full border-2 border-cyan-500/30 rounded-2xl -z-10" />
          </motion.div>
        </div>
      </section>

      {/* Skills / Stack */}
      <section id="skills" className="py-32 px-8 bg-slate-950/50">
        <div className="max-w-7xl mx-auto">
          <h2 className="text-sm font-mono text-cyan-500 mb-4 uppercase">Capabilities</h2>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
            {skills.map((skill, i) => (
              <motion.div 
                key={skill}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ delay: i * 0.05 }}
                className="p-6 bg-slate-900/50 border border-slate-800 rounded-xl hover:border-cyan-500/50 transition-all group"
              >
                <h4 className="text-white font-bold group-hover:text-cyan-400">{skill}</h4>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Projects Grid */}
      <section id="projects" className="py-32 px-8">
        <div className="max-w-7xl mx-auto">
          <h2 className="text-4xl font-bold text-white mb-20 tracking-tighter">SELECTED PROJECTS</h2>
          <div className="grid md:grid-cols-2 gap-16">
            {projects.map((project, i) => (
              <motion.div key={i} {...fadeInUp} className="group">
                <div className="relative aspect-video rounded-3xl overflow-hidden mb-6 bg-slate-900 border border-slate-800">
                  {project.img ? (
                    <img src={project.img} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-700" alt={project.name} />
                  ) : (
                    <div className="w-full h-full flex items-center justify-center"><Layers size={48} className="text-slate-700" /></div>
                  )}
                  <div className="absolute inset-0 bg-gradient-to-t from-[#020617] to-transparent opacity-60" />
                </div>
                <span className="text-cyan-500 text-xs font-bold uppercase tracking-[0.2em]">{project.tag}</span>
                <h3 className="text-2xl font-bold text-white mt-2">{project.name}</h3>
                <p className="mt-4 text-slate-400">{project.desc}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Contact Section */}
      <footer id="contact" className="py-32 border-t border-slate-900">
        <div className="max-w-3xl mx-auto px-8 text-center">
          <motion.div {...fadeInUp}>
            <h2 className="text-5xl font-bold text-white mb-8">Get in Touch</h2>
            <div className="grid gap-6 text-lg">
              <a href="mailto:imichael099@gmail.com" className="flex items-center justify-center gap-3 text-white hover:text-cyan-400 transition">
                <Mail size={20} /> imichael099@gmail.com
              </a>
              <a href="tel:+2349082747887" className="flex items-center justify-center gap-3 text-white hover:text-cyan-400 transition">
                <Phone size={20} /> +234 908 274 7887
              </a>
              <a href="https://github.com/imichael099-eng" target="_blank" rel="noreferrer" className="flex items-center justify-center gap-3 text-white hover:text-cyan-400 transition">
                <Github size={20} /> imichael099-eng
              </a>
            </div>
            <p className="mt-20 text-xs font-mono uppercase tracking-[0.3em] text-slate-600">
              © 2026 S.Y MICHAEL · ALL RIGHTS RESERVED
            </p>
          </motion.div>
        </div>
      </footer>
    </div>
  );
};

export default S_Y_Portfolio;
